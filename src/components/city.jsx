// eslint-disable-next-line no-unused-vars
import React from 'react';
import './City.css';

const City = ({ name }) => (
  <div className="city">
    <h1>{name}</h1>
  </div>

);

export default City;